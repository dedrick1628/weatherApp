// Function to convert temperature from Celsius to Fahrenheit
function celsiusToFahrenheit(celsius) {
    return (celsius * 9/5) + 32; // Conversion formula
}

module.exports = {
    celsiusToFahrenheit // Exporting the function
};
